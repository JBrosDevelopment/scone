struct Holder {
    struct Inner {
        static Inner: Value;
        /////////////////////////
        // FIX PARSER ///////////
        /////////////////////////
    }
    enum Types {
        A, B, C
    }
    Types: Run();
}

Holder::Inner: A = Holder::Inner::Value;

